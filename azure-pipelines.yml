trigger:
  branches:
    include:
      - dev

pool:
  name: 'Default'

steps:
  - script: |
      echo "🚀 Starting auto-deploy on 084 server..."
      cd C:\Users\don\Desktop\DonRyu
      git pull origin dev
      echo "✅ Build complete on 084 server"
    displayName: 'Pull and Build (No Install)'
