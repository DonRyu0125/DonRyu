trigger:
  branches:
    include:
      - dev

pool:
  name: 'Default'

steps:
  - script: |
      echo "🚀 Starting auto-deploy on 084 server..."
      cd C:\deploy\DonRyu
      git pull origin dev
    displayName: 'Pull and Build (No Install)'
